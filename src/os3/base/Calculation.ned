
package os3.base;

//
// Calculation of distances, attenuation and other channel characteristics
//
simple Calculation
{
    parameters:
        @display("i=device/palm");
        string rainTableFile;      // Filename and path to the table containing the parameters for specific rain attenuation
}
